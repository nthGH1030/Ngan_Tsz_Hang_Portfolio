---
interface Props {
	title: string;
	tags: string[];
	achievement?: string;
	id?: string;
}

const { title, tags, achievement, id } = Astro.props;
---

<article
	id={id}
	class="rounded-2xl border border-line bg-surface p-5 shadow-sm md:p-6"
>
	<h3 class="text-lg font-semibold text-ink">
		{title}
	</h3>
	<div class="mt-3 max-w-3xl text-sm leading-6 text-muted">
		<slot name="description" />
	</div>
	<ul class="mt-4 flex flex-wrap gap-2" aria-label="Tech stack">
		{tags.map((tag) => (
			<li class="chip px-2 py-1 text-xs font-medium whitespace-nowrap">
				{tag}
			</li>
		))}
	</ul>
	{achievement && (
		<p class="mt-4 border-t border-line pt-4 text-sm leading-6 text-muted">
			<span class="mb-0.5 block text-[11px] font-bold uppercase tracking-[0.12em] text-accent">
				Impact
			</span>
			{achievement}
		</p>
	)}
	<div class="empty:hidden mt-5">
		<slot name="demo" />
	</div>
</article>
